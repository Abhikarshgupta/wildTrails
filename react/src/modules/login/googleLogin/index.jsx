import { Button } from "@mui/material";

const Index = () => {
	return (
		<Button variant='contained' color='success' style={{ height: "3rem" }}>
			Login With Google
		</Button>
	);
};

export default Index;
